import { StyleSheet } from "react-native";
import { fonts, fonts_sizes } from "@/constants/styles";

export const styles = StyleSheet.create({
	title: {
		fontFamily: fonts.sofiapro_semiBold,
		fontSize: fonts_sizes.emphasis,
		flexShrink: 1,
	},
});
