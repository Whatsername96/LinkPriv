import { colors, fonts, fonts_sizes, spaces } from "@/constants/styles";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		width: "100%",
		paddingBottom: spaces.item_space_min,
		backgroundColor: colors.white_100,
	},
});
